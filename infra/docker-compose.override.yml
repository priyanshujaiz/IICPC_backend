services:
  # Dev overrides: mount source for hot-reload with tsx watch
  telemetry:
    volumes:
      - ../packages/telemetry/src:/app/packages/telemetry/src:ro

  leaderboard:
    volumes:
      - ../packages/leaderboard/src:/app/packages/leaderboard/src:ro